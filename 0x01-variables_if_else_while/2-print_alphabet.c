#include <stdio.h>

/**
 * main - This program prints the alphabet in lowercase.
 *
 * Return: 0.
 */

int main(void)
{
	char d;

	for (d = 'a'; d <= 'z'; d++)

		putchar(d);

	putchar('\n');

	return (0);
}
