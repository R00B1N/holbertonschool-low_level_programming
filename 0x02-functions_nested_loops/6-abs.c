#include "holberton.h"
/**
 * _abs - compute absolute value of integer
 * @n: integer
 *
 * Return: return absolute value for an integer
 */
int _abs(int n)
{
if (n > 0)
return (n);
else
return (-n);
}
